<div class='main'>
    <% @tasks.each do | task | %>
      <div class="sub">
        <p> <%= task.created_at.try(:to_date) %> </p>
        <p> <%= task.category_id %> </p>
        <p> <%= task.body %> </p>

        <%= link_to 'Edit', edit_category_task_path(task.id)/edit, class: 'btn btn-outline-primary btn-sm', method: :get%>
        <%= link_to 'Delete', category_tasks_path(task.id), class: 'btn btn-outline-primary btn-sm', method: :delete%>

        </div>
    <% end %>
</div>

<%= link_to 'Add task', new_category_task_path, class: 'btn btn-outline-primary btn-lg', method: :get%>